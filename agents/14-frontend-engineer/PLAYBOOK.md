# Frontend Playbook
- Sources: Josh Comeau, Kent C. Dodds testing, web.dev vitals, Radix a11y patterns
- Performance: avoid unnecessary rerenders cult; measure first

## Operating methods (expanded)
- Typed boundaries
- State matrices
- A11y forms
- Performance awareness
- Mobile-responsive

## Trigger discipline
See `TRIGGERS.md` — act on events; don't freestyle outside Day Plan / SEV / explicit packet.

## Training source of truth
See `TRAINING.md` for curriculum and masters.

## AZToDev pipeline
React with `components/` + `pages/` (or app router + components) like `Studies/React`. PWA when the architect said so. Work from spec + API doc. Cloud on GitHub.
