# Rakza — בריף למצגת v2 (תיקון לפי הערות ציון)

**Task:** Noa asked (32-delivery-lead / קשת) — ציון לא אהב את המצגת הקיימת (`ops/intake/ideas/IDEA-2026-09-07-inventory-deck.md`, `INV-02`) ורוצה גרסה חדשה.
**Initiative:** `IDEA-2026-09-07-inventory-deck` (אותה יוזמה — לא פרויקט Linear חדש, לא בור WIP חדש).
**Board:** `ops/intake/inventory-deck-board.json` → נוספה `INV-04` (status: `ready`, owner: `22-content-marketing`).
**Owner של המסמך הזה:** 32-delivery-lead (קשת) — תכנון/בריף בלבד. בניית ה-PPTX בפועל היא לא בתפקיד שלי.

---

## מה בדקתי לפני שכתבתי את הבריף

- קראתי את ה-v1 שכבר קיים: `ops/intake/inventory-deck-board.json` (INV-01/02/03), `ops/intake/inventory-audit/screen-catalog.md` (27 screenshots, כל המודולים), `agents/22-content-marketing/outbox/INV-02-job-result.md` (14 שקפים, script, מייל).
- אימתתי שכל 27 ה-screenshots עדיין קיימות ב-`ops/intake/inventory-audit/screenshots/` — **אין צורך בסבב צילום חדש**. כל 8 המסכים שציון ביקש כבר צולמו ב-INV-01.
- מצאתי את מקור ה"איציק": `screenshots/60-sa-companies.png` (מסך Super-admin) מציג בפועל דייר לדוגמה בשם **"איציק סיטונאות"** על גבי המסך עצמו — זה כנראה מה שציון מתכוון אליו ("לא צריך להיות כתוב שם שזה קשור לאיציק"). זה גם מסך שהוא לא ביקש עכשיו בכלל (super-admin/multi-tenant לא ברשימה החדשה שלו).
- מייל ה-INV-02 (`ops/scripts/send-inventory-deck-email.mjs`) כלל בגוף המייל את המשפט "מוכנה להצגה לאיציק" — זה טקסט תפעולי-פנימי (לא בתוך המצגת עצמה), אבל v2 לא אמור לחזור עליו כשמתקשרים איתו על הדק הזה.

## מסקנה: מקור הבעיה

v1 היה **מצגת שיווקית** (UVP, בעיה, תמחור, CTA) עם 14 שקפים כולל landing/login/customers/settings/super-admin. ציון עכשיו מבקש **הדגמת מוצר ממוקדת** — 8 תרחישים ספציפיים, בלי שיווק מסביב, ובלי שום דבר שמזהה לקוח ספציפי על המסך.

---

## מבנה מוצע ל-v2 — 10 שקפים (במקום 14)

סדר לפי הבקשה המדויקת של ציון. כל שורה: screenshot קיים → מה להראות → callout (רק אם צריך, "טיפה").

| # | שקף | Screenshot (קיים) | מה מראים | Callout קטן? |
|---|------|-------------------|-----------|---------------|
| 0 | שער | — | לוגו/שם מוצר בלבד ("Rakza — ניהול מלאי והפצה"). **בלי** שם לקוח, בלי "מוכן להצגה ל-X". | לא |
| 1 | דשבורד ראשי | `10-admin-dashboard.png` | KPI חודשי, סרגל סטטוס (פתוחות/מלאי נמוך/בדרך), גרפים | ✅ חץ קטן על "מלאי נמוך" — זו הסיבה שהמנהל פותח את המערכת כל בוקר |
| 2 | מסך הזמנות | `11-admin-orders.png` | רשימת הזמנות + סינון סטטוס | לא |
| 3 | איך נראית הזמנה | `12-admin-order-detail.png` | פירוט: לקוח, פריטים, סטטוס, הקצאת נהג, היסטוריה | ✅ מסגרת קטנה סביב "הקצאת נהג" — מקשר קדימה לשקף 8 |
| 4 | מסך מוצרים | `13-admin-products.png` | קטלוג SKU, ברקוד, מלאי, סימון מלאי נמוך | לא |
| 5 | איך נראה מוצר | `14-admin-product-edit.png` | טופס מוצר: יחידות/קרטונים/משטח, מחירים, price breaks, bin ברירת מחדל | ✅ תג קטן ליד "Price breaks" — פיצ'ר שמובדל מהתחרות (אקסל/וואטסאפ) |
| 6 | מסך המחסן | `17-admin-warehouse-map.png` | מפת שורות A–G, bins צבעוניים (ריק/מלאי/אוזל/מלא) | ✅ מקרא צבעים קטן בפינה (כבר קיים במסך — רק להבליט בעיצוב השקף) |
| 7 | נכנסים למדף מסוים | `18-admin-warehouse-row.png` | תצוגת שורה בודדת אחרי לחיצה — מדפים ימין/שמאל, תוויות | ✅ חץ קטן: "לחיצה על תא בודד → ניהול מלאי מדויק בכל תא" (מפנה למסך `19-admin-warehouse-bin.png` בלי שקף נפרד, רק כהערה) |
| 8 | ניהול צוות | `20-admin-users.png` | ניהול משתמשים: admin/agent/warehouse/driver, הפעלה/השבתה | לא |
| 9 | סוכן עושה הזמנה | `30-agent-sales.png` | wizard 3 שלבים: לקוח → מוצרים → סיכום | לא |
| 10 | מלקטים ומעדכנים | `41-warehouse-order-pick.png` | צ'קליסט ליקוט, סימון פריט, השלמת ליקוט | ✅ מסגרת קטנה סביב כפתור "הקצאת נהג" בתחתית המסך — הרגע שבו ההזמנה "משתבצת" לנהג |
| 11 | הנהג רואה את זה | `50-driver-deliveries.png` | לוח יומי, "המסלול להיום", עצירות פתוחות, סימון מסירה | לא |

**סה"כ:** שער + 10 שקפי מסך = 11 שקפים (לעומת 14 ב-v1). זמן משוער: ~5–6 דק' (קצב דומה ל-v1, פחות שקפי שיווק).

### שקפים שיוצאים לגמרי מ-v2 (היו ב-v1)

| שקף v1 | Screenshot | למה יוצא |
|--------|-----------|----------|
| הבעיה (Excel/WhatsApp) | landing hero | לא ביקש — זו שכבת שיווק, לא הדגמת מסכים |
| 4 תפקידים (סיכום) | — | מיותר; המסכים עצמם מדגימים את זה |
| Super-admin multi-tenant | `60-sa-companies.png` | **מציג את שם "איציק סיטונאות" על המסך** — זה בדיוק מה שציון ביקש להוריד. גם לא חלק מהבקשה החדשה |
| CTA + pricing | landing | לא ביקש; זו לא מצגת מכירה עכשיו, זו הדגמת מוצר |

אם ציון בכל זאת רוצה שקף פתיחה/סגירה שיווקי — זו שאלה פתוחה אליו, לא הנחה שלי (ראה "השאלה הפתוחה" למטה).

---

## דגשים לעיצוב ה-callouts ("טיפה שצריך")

ציון ביקש **מעט** — לא להעמיס. כללים ל-22-content-marketing:
- callout = מסגרת/חץ/תג קטן אחד לכל שקף, לא יותר. בלי בלוקים של טקסט מעל הצילום מסך.
- רק ב-5 מתוך 11 השקפים (מסומן בטבלה למעלה) — לא בכולם.
- צבע אחיד לכל ה-callouts (למשל TEAL או GOLD מתוך הפלטה של v1) כדי שיהיה עקבי.
- **איפה שאסור callout בכלל:** בלי שום טקסט/הערה שמזכירה שם חברה, שם לקוח, או "דמו ל-X". גנרי בלבד.

---

## בדיקת "בלי איציק" — checklist ל-QA לפני שליחה

- [ ] אין screenshot של מסך Super-admin/multi-tenant בדק (הוא היחיד שמציג את השם על המסך).
- [ ] שם הקובץ אינו מכיל שם לקוח (למשל `Rakza_הדגמת_מוצר_v2.pptx` — לא `..._איציק...`).
- [ ] טקסט השער/הסגירה בדק גנרי — שם המוצר (Rakza) בלבד, לא שם חברה/לקוח.
- [ ] אם נשלח מייל על ה-v2 — גוף המייל לא חוזר על "מוכן להצגה לאיציק" כמו ב-v1 (`ops/scripts/send-inventory-deck-email.mjs` הישן).
- [ ] נתוני ה-seed שמופיעים בצילומים האחרים (`10`, `11`, `12`...) נבדקו — לא מצאתי אזכור נוסף של "איציק" בשום screenshot חוץ מ-`60`, אבל שווה שה-QA (22-content-marketing) יסתכל שוב ברזולוציה מלאה לפני שליחה.

---

## מה עדיין לא בתפקיד שלי (חוצה תפקיד → DELEGATE)

1. **בניית ה-PPTX בפועל** (עיצוב, python-pptx build, script חדש) — זה 22-content-marketing (רוני), לא Delivery Lead. הבריף הזה הוא הקלט שלו.
2. **הנחת הקובץ הסופי בתיקיית שולחן העבודה של ציון** (`Desktop/ניהול מלאי/05-מצגת-שיווק/`) והחלפת ה-v1 הישן שם — זו כתיבת דיסק PC, שייכת ל-34-pc-ops (נדב), לא ל-Cloud. לפי המדיניות: "PC disk/GitHub push מתיקייה מקומית → DELEGATE: 34-pc-ops".
3. **שליחת מייל** עם ה-v2 (אם רוצים) — מוגדרת כפעולת "כתיבה"/מוטציה שדורשת PIN לפי `_company/FACTORY.md` (טבלת PIN: "שליחת מייל" = **עם PIN**). לא נשלח שום מייל בסבב הזה בלי PIN מפורש.
4. **Linear** — `INV-03` עדיין חסום (`LINEAR_API_KEY`/MCP לא זמינים בסשן). עדכנתי רק את קובץ ה-board המקומי (`ops/intake/inventory-deck-board.json`), לא Linear עצמו. לא ממציא קישור Linear.

**DELEGATE: 22-content-marketing | INV-04 — בנה מצגת v2 (11 שקפים) לפי הבריף הזה (`agents/32-delivery-lead/outbox/2026-09-09_inventory-deck-v2-revision-brief.md`) ו-`ops/intake/inventory-audit/screen-catalog.md`. שימוש חוזר ב-15 screenshots קיימות (ראה טבלה). בלי screenshot `60-sa-companies.png`. Callout קטן רק ב-5 שקפים המסומנים. שם קובץ גנרי, בלי "איציק". פלט: `ops/outbox-founder/2026-09-09_rakza-product-demo-v2-he.pptx` (לא ל-git — gitignored, כרגיל).**

**DELEGATE: 34-pc-ops | אחרי ש-22-content-marketing מסיים v2 — למשוך את ה-PPTX החדש מ-GitHub, למקם בתיקיית `Desktop/ניהול מלאי/05-מצגת-שיווק/` ליד/במקום ה-v1 הישן (Rakza_מצגת_שיווק_7דק.pptx), ולדווח outbox עם הנתיב הסופי. קריאה+כתיבת דיסק בלבד, לא צריך PIN לעצם ההעתקה לתיקייה מקומית.**

---

## שאלה פתוחה לציון (לא ניחשתי בשמו)

בקשתו כללה 8 תרחישים מדויקים ולא ציין אם הוא רוצה שקף פתיחה/סגירה כלשהו מעבר לזה. הצעתי כאן שער מינימלי בלי שיווק (שם מוצר בלבד). אם הוא רוצה גם משפט UVP קצר בפתיחה או CTA בסוף — זה יתווסף בקלות אחרי אישור, לא חוסם את הבנייה של 10 שקפי המסך.

---

## סטטוס WIP

זו לא משימת WIP חדשה — היא המשך של יוזמה קיימת שכבר הייתה בשלב P3 (חסום ב-Linear). ה-`activeWork` הראשי בפועל (`pc-production-inventory`) לא נגע ולא הוחלף — INV-04 היא thread נפרד קטן, לא מתחרה עם נדב/34-pc-ops על אותה עבודה כרגע (הדלגציה ל-34-pc-ops כאן היא שלב מאוחר, אחרי שרוני מסיימת).

---

LEARNING:
- do: כשציון נותן פידבק שלילי ("לא אהבתי") על תוצר קיים, קודם למצוא בקבצי הראיה הקיימים (board/outbox/screenshots) *מה בדיוק* בתוצר עלול להיות המקור — כאן: זיהוי חי ש-screenshot ה-super-admin מציג שם לקוח על המסך עצמו — לפני שכותבים בריף ניחוש. זה חסך צילום מסכים מיותר כי כל 8 המסכים החדשים כבר קיימים מ-INV-01.
- dont: אל תבנה את ה-PPTX/עיצוב בעצמך בתפקיד Delivery Lead רק כי זה נראה מהיר — זה תפקיד 22-content-marketing; התפקיד שלך הוא הבריף + ה-board + ה-DELEGATE, לא הכלי של מומחה אחר.
- note: מצאתי שכל 27 screenshots המקוריות מ-INV-01 עדיין קיימות ב-git (`ops/intake/inventory-audit/screenshots/`), כולל כל 8 המסכים שציון ביקש עכשיו — v2 יכולה להיבנות מיד בלי סבב צילום נוסף. עדכנתי `ops/intake/inventory-deck-board.json` עם `INV-04` (status ready) והנפקתי שני DELEGATE — 22-content-marketing לבנייה, 34-pc-ops להנחה בתיקיית שולחן העבודה — במקום לנחש ולבנות תוכן PPTX עצמי מחוץ לתפקיד.
