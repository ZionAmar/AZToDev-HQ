# 05-db — cake-recipe-demo

**Status:** N/A — no database.

This bet is a static content page. All recipe text and structure are embedded in HTML (or a single JSON file inlined at build time if Dafna prefers — still no server DB).

**Architect decision:** No ERD. If persistence is requested later, stop and escalate to Keshet before adding MySQL.
